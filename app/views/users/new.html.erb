<h1>Crear usuario</h1>

<%= form_with(model: @user, local: true) do |form| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this patient from being saved:</h2>

      <ul>
        <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.email_field :email %>
  </div>

  <div class="field">
    <%= form.label :role %>
    <%= form.select :role, options_from_collection_for_select(Role.all, :id, :name) %>
  </div>

  <div class="field">
    <%= form.label :profile_type %>
    <%= form.select :profile_type, [[], ['CESO', 'CesoProfile'], ['Empresa', 'BusinessProfile']], {}, id: 'profile' %>
  </div>

  <div id="profile_fields">
    <div id="loader">
    </div>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


<%= link_to 'Index', root_path %>
